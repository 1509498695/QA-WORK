# qa-case-xlsx 领域语言

qa-case-xlsx 是一个独立的测试用例生成与交付系统。本文件只定义系统内的统一术语，不记录实现状态或实施步骤。

## 系统边界

**独立用例系统**：
以 `qa-case-xlsx` 为唯一用户入口、拥有自身工作区身份与审计状态的测试用例系统。它可以使用 Codex 基础运行时和 LG Feishu 平台能力，但不消费 QAWORK 的代码、命令、目录、模板绑定或私有状态。
_Avoid_：本地 Skill、QAWORK 子模块、qa-case 附属流程

**系统工作区**：
独立用例系统唯一拥有的运行与证据边界，用于区分其来源、任务、授权状态和交付记录。
_Avoid_：QAWORK 工作区、临时目录

**QAWORK 独立性**：
独立用例系统的输入、处理、授权、交付和恢复均不需要 QAWORK 存在或可用。
_Avoid_：复用 QAWORK、从 QAWORK 启动

## 来源与证据

**策划案来源**：
用户本次明确提供、且能被系统完整读取和定位的一份需求材料。
_Avoid_：参考资料、外链

**本地来源**：
由用户上传或明确指定的本地 DOCX、PDF、XLSX、Markdown、TXT 或图片文件。
_Avoid_：离线文档

**飞书来源快照**：
在特定读取时点从飞书文档完整固化的只读来源，包含正文、结构、表格、图片、附件及完整性状态。只有完成状态的快照才能作为正式证据。
_Avoid_：飞书链接、在线正文、临时下载

**策划案源包**：
同一次生成任务中全部本地来源与飞书来源快照的集合；各来源保持独立身份，不因标题或内容相似而合并。
_Avoid_：合并文档、输入文件夹

**来源证据**：
能够回溯到具体来源及其段落、页面、表格、单元格、文档块或图片位置的业务事实依据。
_Avoid_：模型判断、常识补充

**源包冲突**：
不同来源对同一业务事实给出互斥结论的状态；冲突解决前相关内容保持待确认。
_Avoid_：版本覆盖、最新文档优先

**旧玩法基线缺口**：
来源只声明沿用或复用旧玩法，却没有提供可验证旧行为的证据缺口。
_Avoid_：默认继承、历史常识

## 用例与审计

**完整用例集**：
未预设条数、由已确认事实与适用风险维度自然展开，并能逐条回溯来源的用例集合。
_Avoid_：固定数量用例、示例用例

**待确认草稿**：
来源不完整、证据冲突或关键事实未确认时允许形成的非正式用例产物。
_Avoid_：正式用例、交付完成

**审计包**：
记录来源、事实、生成决策、规则应用、映射、校验、写入和回读证据的可复核产物集合。
_Avoid_：日志目录、中间文件

**正式规则包**：
经确认并受版本与哈希约束的个人测试规则集合；运行过程不得自动从历史材料改写它。
_Avoid_：经验库、候选规则

## 飞书交付

**指定用例模板**：
用户明确选定、由独立用例系统在自身飞书 Provider 边界内登记并验证的只读复制母版。原模板不被修改；从它复制并完成登记的新表才属于系统自有用例表。
_Avoid_：QAWORK 模板绑定、交付目标表、本地模板

**模板合同**：
用户已接受的指定用例模板结构与交付语义。每次复制前都必须证明当前模板仍符合该合同，模板自身的后续变化不会自动成为新合同。
_Avoid_：当前模板、视觉相似、自动更新

**模板漂移**：
指定用例模板的当前状态不再符合已接受模板合同的情形。模板漂移会阻断新交付，直到变化被重新核验并明确接受。
_Avoid_：兼容更新、自动升级

**系统自有用例表**：
由独立用例系统为一次首次交付从指定用例模板复制、新建并登记的 AI-owned 飞书表格。系统对其拥有可证明的创建来源与后续修订资格。
_Avoid_：用户表格、外部模板、任意在线表

**受控修订**：
只针对系统自有用例表、绑定当前内容状态并在写后完成回读的后续修改。
_Avoid_：覆盖、原位改表、直接更新

**外部既有表**：
不是由独立用例系统创建并登记的飞书表格；它可以作为明确允许的读取来源，但不是系统可修改的交付对象。
_Avoid_：目标表、可写表

**正式飞书交付物**：
本地生成门禁、飞书写入和线上精确回读全部通过的系统自有用例表。
_Avoid_：本地工作簿、飞书预览、已创建表格

**本地恢复包**：
每次飞书写入前必须完成的本地工作簿与审计包组合，用于证明生成结果并支持失败恢复，但不单独代表正式交付完成。
_Avoid_：正式交付物、临时文件、可选备份

**已交付**：
正式飞书交付物已经通过线上语义回读，且其内容与本地恢复包中的已校验结果一致的任务状态。
_Avoid_：本地成功、写入成功、已创建
