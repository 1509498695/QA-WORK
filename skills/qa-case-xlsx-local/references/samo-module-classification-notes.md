# SAMO 模块分类说明

当前正式快照包含 210 个 SAMO 模块，来源为用户已确认的 2026-07-24 本地快照。普通用例生成只读取本 Skill 内的离线快照，不依赖或调用其他业务 Skill。

业务分类、对象域和个人注解必须由当前策划案证据或用户本次确认触发；模块名称只用于精确命中和候选召回，不能单独形成分类结论。本文件不保存第二份模块正文，完整清单以 `samo-project-modules.json` 为唯一事实源。
